
import React from 'react';

const SubSectionTitle = ({ children }: { children: React.ReactNode }) => (
  <h4 className="text-xl font-semibold mt-6 mb-3 text-red-300">{children}</h4>
);

export default SubSectionTitle;
